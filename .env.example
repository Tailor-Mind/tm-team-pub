# Apps Script Web App URL (deployed from private repo via clasp).
# Used by CompReveal.astro to log reveal events to Sheets.
PUBLIC_TRACK_ENDPOINT=

# Google Form embed URLs (per language).
PUBLIC_APPLY_FORM_URL_EN=
PUBLIC_APPLY_FORM_URL_ES=
